// Placeholder progress indicator for the vocabulary test.
type Props = {
  current: number;
  total: number;
};

export default function TestProgress(_props: Props) {
  return <div>Test progress indicator is not connected yet.</div>;
}
